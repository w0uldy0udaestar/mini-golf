# HANDOFF.md

## 현재 상태 (2026-08-14)

M3(폴리시) 막바지 — 사운드·이펙트·디자인 리뷰 게이트 통과 후, 사용자 실플레이 피드백으로 모션 품질을 집중 개선 중. 브랜치 `feature/m3-sound-fx` (main 미머지, 전부 push됨). 빌드·SwiftFormat·테스트 16개 통과 상태.

실행: `swift build && .build/debug/MiniGolf` (⛳️ 좌클릭 = 재개/일시정지, 우클릭 = 메뉴)

## 완료 (M3 구간)

- [x] 사운드: 합성음 SoundKit(AVAudioEngine, 번들 증가 0) — 클럽별 타격·휘시·착지·립아웃·홀인·워터·차임. 메뉴바 사운드 토글. **사용자 청음 소감은 아직 못 받음**
- [x] /design-review 게이트 PASS (A-/A) — 밝은 배경 대응은 '고대비 모드' 토글(기본 꺼짐, 사용자 미학 결정), Palette 상수 도입. 보고서: `~/.gstack/projects/mini-golf/designs/design-audit-20260814/`
- [x] 전역 단축키 제거 → 메뉴바 ⛳️ 좌클릭 토글 (핫키 사용자 설정은 IDEAS)
- [x] 클럽별 렌더 차등(길이 DR45→PT31·헤드 모양·로프트 블레이드 틸트) + 캡슐 기하 morph(종류 전환) + 전 파라미터 지수 스무딩 + 연속 rate 램프 — 클럽 변경 움찔 완전 제거 (여러 라운드의 사용자 피드백 반복으로 도달)
- [x] 홀인 드롭 연출 (공이 컵으로 가라앉음, 렌더 가드)
- [x] **통합 리그(Rig) 재구축** — 스윙·걷기·전환이 단일 파라미터 공간, 두 팔 1급(트레일 암 옅은 톤), 발끝 피니시
- [x] **보행 리서치 기반 재작성** (`docs/`엔 없음 — 커밋 ed27be5 메시지·세션 로그 참고): 접지점 월드 래치 + stride warping(보폭∝√v) + duty 가변 + sin² 착지 + C1 밥 + 저속 팔 감쇠. 사용자 판정 "자연스러워졌다"
- [x] 랜덤 걷기 모션 20종 (모듈레이션 채널 + 시간 엔벨로프, 걷기당 최대 4개 + 어깨 캐리)
- [x] 여유 걷기 dist/10 (최대 12s)

## 다음 단계 (재개 지점)

1. **진행 중**: 스윙·퍼팅·임팩트 이론 리서치 (서브에이전트 실행 중) → 결과 종합 후 적용 여부 사용자 협의. 예상 주제: kinematic sequence·손목 래그·Tour Tempo 3:1·hitstop·follow-through
2. 사운드 청음 소감 수령 → 튜닝
3. 사용자 최종 OK 시 `feature/m3-sound-fx` → main 머지, M3 종료
4. M4: README(고대비 모드·xattr 안내)·데모 GIF·MIT LICENSE → **public 전환은 사용자 확인 게이트**

## 주요 결정과 이유 (최근)

- **모션 아키텍처**: 별개 리그 교체 → 단일 Rig 공간 + 지수 감쇠 추적 (전환 순간이동 구조적 제거). 속도 전환은 반드시 연속 램프(계단식 rate 변경 = 가속 킥 = 움찔)
- **발 미끄러짐**: 곱셈 압축(gather)·저역 필터가 접지발을 끌면 안 됨 — 접지점 래치 + 보폭 워핑이 정답 (Epic Speed Planting/Pose Warping, walk ratio 문헌)
- **사용자 모션 감각이 매우 예리함** — "웨지-퍼터는 자연스러운데" 같은 대조 피드백이 진단의 결정타가 됨. 수정 후 반드시 실플레이 판정 받기

## 주의사항·리스크

- 검증 명령은 파이프(`| tail`) 금지 — 종료 코드가 가려져 빌드 실패 커밋 사고 있었음(881f49a→093708e). 종료 코드 직접 확인
- 합성 키(osascript) 전송 금지 — 메모리 `overlay-synthetic-key-hazard` 참고
- 배경은 사용자 영역 · 스틱맨 감성은 범위 축소 금지 (메모리 참고)
- 저장소 private. public 전환은 M4 사용자 게이트
