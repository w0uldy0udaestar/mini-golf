# HANDOFF.md

## 현재 상태 (2026-08-14, 세션 종료 시점)

M3 폴리시 막바지. 사운드·디자인 게이트·클럽 연출·스윙 리서치(3회)까지 반영됐고, **걷기↔스윙 전환 품질에서 사용자 불만족 상태로 세션 종료**. 브랜치 `feature/m3-sound-fx`(HEAD `98ec610`, 전부 push, main 미머지). 빌드·SwiftFormat·테스트 18개 통과.

실행: `swift build && .build/debug/MiniGolf` (⛳️ 좌클릭 = 재개/일시정지, 우클릭 = 메뉴)

## ⚠️ 최우선 재개 지점: 모션 전환 구조 개편

전환(스윙↔걷기) 문제를 3차례 패치로 개선했으나 사용자 판정은 "아직 어색함". 마지막 리서치(아래 참고)의 결론은 **패치가 아니라 구조 개편**:

1. **1차 지수 감쇠 추적기 → 임계 감쇠 스프링**(속도 상태 보존, Holden Spring-It-On 정해 코드가 리서치 보고에 있음) + **타깃 속도 피드포워드**(rate 220 불필요해짐)
2. **rate를 모드별(5/13/14/22/45/60/220) → 채널별 상수**(발 0.02s·몸통 0.06s·손 0.05s·자유팔 0.09s·클럽 0.11s halflife)로 — 모드 전환이 타깃만 바꾸게
3. 이번 세션 마지막 커밋(98ec610)의 원칙 유지: **접지된 발·무릎은 어떤 블렌드·필터에도 섞지 않는다**

이건 별도 작업 단위 규모 — **Code Reviewer 리뷰까지 붙여서 진행 권장**. 현재 3층 구조(게이트 + 도착/진입 블렌드 + 모드 크로스페이드)가 누적 패치로 복잡해져 있어, 스프링 전환 시 크로스페이드·arrival 블렌드는 단순화/제거 후보.

## 리서치 자산 (이 세션의 핵심 산출물 — 세션 로그에 전문 있음)

3건의 opus 리서치 보고 (커밋 메시지 `ed27be5`·`60a197b`·`3eccc4f`에 요약):
1. **보행**: footskate 이론, Epic Distance Matching/Stride Warping/Speed Planting, Holden foot locking, walk ratio(보폭∝√v) → 적용 완료, 사용자 "자연스러워졌다"
2. **스윙**: kinematic sequence(골반→몸통→팔→클럽 u³), Tour Tempo, 퍼터 펜듈럼(PGA 317ms), 임팩트 동기(P1), hitstop(적용 후 사용자 판정 '렉' → 제거·재도입 금지) → 적용 완료
3. **전환·게임플레이**: gait initiation(위상 duty 시작), Bollo Inertialization, 최소 저크(Flash&Hogan), 경사 라이 0.7 비율, 펀치샷 프로필, 파워-정확도(베이스 0.25 + overdrive^1.6·정규분포) → 적용 완료, **스프링+채널 rate만 미적용(위 재개 지점)**

## 완료 (M3 누적)

- [x] 합성음 SoundKit + 사운드 토글 (**청음 소감 아직 못 받음** — 다음 세션에서 확인)
- [x] /design-review 게이트 PASS (A-/A), 고대비 모드 토글(기본 꺼짐), Palette
- [x] 메뉴바 ⛳️ 버튼 토글 (전역 단축키 제거, 사용자 설정 핫키는 IDEAS)
- [x] 통합 리그(Rig) + 두 팔 + 클럽별 렌더(길이·헤드 morph) + 클럽 변경 무움찔(스무딩+연속 램프)
- [x] 거리 기반 발 디딤 걷기(래치·stride warping) + 랜덤 모션 20종 + 여유 템포(dist/8·최대 12s)
- [x] 스윙 운동 사슬·임팩트 동기·퍼터 펜듈럼·스미어·공 스트레치·오버랩·무빙 홀드
- [x] 홀인 드롭 연출, 경사 라이(스탠스+물리 0.7 정합), 벽 펀치샷(하한 0.55), 풀파워 리스크
- [x] GolfCore 테스트 18개 (경사·미스샷 포함)

## 주요 결정·교훈 (이 세션)

- **hitstop 재도입 금지** — 골프의 단일 연속 동작에선 정지가 '렉'으로 읽힘 (사용자 판정, 3c4d6ca)
- **재그립 딥·잔상 페이드 금지** — 사용자 기각. 헤드 전환은 캡슐 morph가 정답 (a83fcea)
- **계단식 rate 전환 = 가속 킥 = 움찔** — 모든 속도 변화는 연속 램프로 (6fdb09a)
- **접지발은 신성** — 필터·블렌드·곱셈 보정 어느 것도 통과 금지 (98ec610)
- **검증 명령에 파이프 금지** (`| tail`이 종료 코드를 가림 — 빌드 실패 커밋 사고 있었음). `BUILD_EXIT=$?` 패턴 사용
- 사용자 모션 감각이 예리함 — 대조 피드백("웨지-퍼터는 괜찮은데")이 최고의 진단 도구. 수정마다 실플레이 판정 필수

## 다음 단계 (순서 제안)

1. **모션 전환 구조 개편** (위 최우선 재개 지점) → 실플레이 판정
2. 사운드 청음 소감 → 튜닝
3. 사용자 OK 시 main 머지, M3 종료
4. M4: README(고대비 모드·xattr)·데모 GIF·LICENSE → **public 전환은 사용자 확인 게이트**

## 주의사항

- 합성 키(osascript) 전송 금지 — 메모리 `overlay-synthetic-key-hazard`
- 배경은 사용자 영역 · 스틱맨 감성은 축소 금지 — 메모리 참고
- 저장소 private, main은 M2+quiet-hud 상태로 동작 보장됨
