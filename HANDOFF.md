# HANDOFF.md

## 현재 상태 (2026-08-15 밤 — M3 종료)

**M3 main 머지 완료(887bc45)**, LICENSE·README·CHANGELOG 정비됨. 남은 것: ①최신 반영분(철푸덕 넘어지기·스코어 감정 반응·아이들 잔동작) 실플레이 판정 ②데모 GIF(README "준비 중") ③**GitHub public 전환 — 사용자 명시 확인 후에만**. 이하는 세션 상세 기록.

## 이전 상태 기록 (2026-08-15)

M3 대확장 완료 직후. 브랜치 `feature/m3-sound-fx` (전부 push, main 미머지). 빌드·SwiftFormat·**테스트 31개** 통과, Code Reviewer 2회(스핀물리 배치·코스개편 배치) 지적 전부 반영. **오늘 후반 기능들의 실플레이 종합 판정 대기 중.**

실행: `swift build && .build/debug/MiniGolf` (⛳️ 좌클릭 재개/일시정지 · 우클릭 메뉴)
관찰 도구: `--demo`(자동 플레이) `--demo-wall`(벽 시나리오) `--demo-card`(스코어카드) `--no-wall-clamp`(침범 재현) `--seed N`(코스 시드 고정 — 예: 64는 1번 홀에 나무+바위+워터+벙커) — 데모 중 경계 침범은 stdout `OUTBOUND`, 조준 스탠스는 `AIM` 로그로 감시

## 8/15 세션 요약 (시간순)

1. **모션 전환 대실패 → 복원**: 스프링 전환 개편(HANDOFF 재개 지점이었음)이 실플레이 3연속 기각 → `3c4d6ca` 코드로 전면 복원(92aa248). 스프링 리서치·게이트 동기·원점 재명명은 `exp/transition-lab-20260814` 브랜치에 보존. **교훈: 모션은 프레임 캡처로 직접 보고 고칠 것** — vInst 불일치·relax 드리프트가 "다리 따로 몸 따로"의 진범이었음 (기각된 개편에서 확인, 현 코드엔 미적용 상태로 남아 있음!)
2. **기능 대량 추가** (전부 사용자 요청, 커밋 로그 참조):
   - 스핀·바운스 물리 정밀화 (opus 리서치 → `docs/research-spin-bounce.md`): Biber (5/7,2/7) 접지 해 + Penner β(낙하 강도 비례 — 탱탱볼 회귀 테스트 있음) + 스핀 인플레 제거
   - 좌우 미러 홀 · 풀파워 미스샷(overdrive^1.6) · HUD 하단 스트립 · 실카드 스코어카드 · 클럽+파워 캐릭터 표시
   - **벽 = 파워 제약 아님**: 화면 밖 금지(렌더 경성 클램프 — 모든 관절+클럽 팁+머리) + 극복 자세(뒷발 벽 딛기·컴팩트 폼·펀치 탄도) + 벽 릴리프 46px
   - 자동 클럽(파4·5 DR / 파3 7I / 그린 퍼터) · 홀 거리 백·화이트 중간(파4 290~400m)
   - 경사 라이 복원(Phys.stanceSlopeRatio 0.7) + 벙커/러프/캐노피 자세
   - **CourseStrategy**: 클럽 도달 거리를 물리 시뮬로 실측(DR 306m)해 해저드를 낙하 지대에 전략 배치, 레이업 안전선 테스트 보장
   - 장애물: 나무(캐노피만 충돌 — 밑 펀치 통과, 트렁크는 화면 뒤편) · 바위(원호 법선 반사)
   - 지형 다이나믹(-7~11m, 내리막 티샷 30%·솟은 그린 35%) + groundBase 적응형(HUD 침범 불가)

## ⚠️ 재개 지점

1. **1차 실플레이 판정 완료 (8/15 후반)**: 사운드 OK. 지적 4건 반영 — ①라이 시각 구분 ②경사 스탠스(applySlopeStance) ③포커스 상실 시 홀드만 해제 ④장애물 가시성(렌더 강화+크기 상향)
2. **2차 요청 5건 반영 완료 (8/15 저녁)**: ①코스 다이나믹(성격 롤+능선·분지·플래토 오버레이+솟은/낮은 그린 — 낙차 평균 12.1m 실측) ②클럽 키 반전(→=드라이버 쪽) ③홀인 스코어 토스트 깃발 위 ④걷기 자세 직립 교정(숙임 제거·팔 최소 진폭) ⑤랜덤 모션 100종(WalkFlavors.swift — 채널 6개 추가, 발 게이트 불변). GitHub는 기존 w0uldy0udaestar/mini-golf private으로 충분 판정
3. **2차 반영분 실플레이 재판정 대기** — 특히 걷기 자세 체감, 모션 다양성·재미, 코스 다이나믹 정도, 깃발 위 스코어
4. **종합 QA 완료 (8/15 밤)** → `docs/qa-report-2026-08-15.md`. 4관점 리뷰(fable 코드·UI·재미·준비도)+물리 소크 3,200샷+밸런스 봇 360홀. 치명 버그(비종결 굴림) 발견·수정. 남은 P0: 밝은 배경 가독성 방향 결정(사용자) · P1: 스코어 감정 반응+아이들 모션, HUD 표고차 · P2: M4 문서
4. 사용자 OK 시 main 머지, M3 종료 → M4 (README·데모 GIF·LICENSE·public 전환 게이트)
5. ~~걷기 게이트 vInst 불일치~~ **정정(8/15 재조사): 현 코드는 불일치 없음** — 위치 smoothstep + 도함수 6u(1-u)로 자체 정합. exp의 45%→0%는 '위치를 smootherstep으로 바꾼 상태'에서의 도함수 동기였음. 도함수 한 줄만 이식하면 오히려 불일치가 생긴다. 걷기를 최소 저크(smootherstep)로 바꾸고 싶을 때만 위치+도함수+dur(dist/8) 세트로 이식할 것

## 주요 결정·교훈 (오늘)

- **모션/시각 주장은 증거로**: 사용자가 "어떻게 확인했는지 가져와" — --demo + screencapture 프레임 + 계측 로거(OUTBOUND)가 표준 검증 루프가 됨
- **기하 추정 대신 구조적 보증**: 벽 침범을 두 번 놓친 뒤(팔 폭만 계산, 클럽 길이 누락) 렌더 직전 경성 클램프로 전환 — 이후 재발 0
- 2D 사이드뷰에서 나무 트렁크는 비충돌이어야 펀치 극복이 성립 (테스트가 설계를 강제)
- 드라이버 306m 실측 → 홀 거리를 백·화이트 중간으로 (사용자 결정)
- 전환 스프링·hitstop·재그립 딥은 재도입 금지 목록 유지

## 주의사항

- 합성 키 전송 금지(메모리 참조) · 검증 명령에 파이프 금지(`$?` 가림) · 캡처는 화면 잠금 시 실패(재시도 루프 사용)
- SourceKit 진단은 상시 뒤처짐 — 컴파일러 결과만 신뢰
- 저장소 private · main은 M2+quiet-hud 상태로 동작 보장
