# PLAN.md — mini-golf

macOS 데스크탑 화면 위 투명 오버레이에서 플레이하는 **사이드뷰 미니골프**.
화면 하단이 지면이 되고, 공이 클럽별 로프트·백스핀에 따른 실제 탄도 물리로 데스크탑을 가로질러 날아간다.
키보드 전용 조작. 오픈소스(MIT) 공개 예정.

- **목적/대상**: 재미·학습 / 누구나 즐기는 캐주얼
- **레퍼런스**: Threads에서 화제가 된 스틱맨 데스크탑 야구 오버레이 (원본 미확인 — `docs/research-tech-stack.md` 참고)
- **MVP 성공 기준**: 9홀 순회 + 타수·파 점수 시스템이 매끄럽게 동작

## 확정 결정사항 (2026-08-13 인터뷰·리서치)

| 항목 | 결정 | 이유 / 검토한 대안 |
|---|---|---|
| 시점 | 사이드뷰 (화면 하단 = 지면) | 탄도·백스핀이 눈에 그대로 보임, 방향 조준 불필요 → 키보드 전용과 정합. 대안: 탑다운(탄도 간접 표현, 조준 키 추가 필요) |
| 조작 | 키보드 전용 | 마우스 클릭 통과 난제가 통째로 사라짐. 대안: 마우스 게이지(hitTest 선택적 통과 필요) |
| 클럽 | 풀 백 13종, 데이터 테이블 기반 | 클럽별 탄도·백스핀 물리가 게임의 핵심 재미. 대안: 3종 축소(전략성 부족으로 기각) |
| 물리 | 자체 탄도 엔진 (순수 Swift) | 마그누스 양력·스핀 감쇠는 SpriteKit 내장 물리로 불가. 대안: SpriteKit 물리(백스핀 표현 불가로 기각) |
| 스택 | Swift/AppKit + SpriteKit(렌더 전용) | 네이티브 오버레이 API, 번들 <1MB, 유사 프로젝트 검증 패턴. 대안: Electron(메모리·GPU 부담), Godot(통과 API 제약) |
| 구조 | SPM 패키지 + Makefile로 .app 조립 | `swift build` CLI 빌드로 기여 장벽↓. 대안: .xcodeproj(diff 노이즈) |
| 저장소 | private 시작 → MVP 때 공개 | 공개 전 품질 통제. 공개 전환은 사용자 확인 게이트 |
| 홀 배치 | 파 구성(합 36) 셔플 + 화이트 티 실거리 (파3 110~170m·파4 250~360m·파5 430~500m) | 실제 골프 감각 (2026-08-13 사용자 피드백). 홀마다 화면 스케일 가변 |
| 디자인 | 게임 요소는 회색 베이스(스틱맨 포함), 포인트 레드는 깃발 하나만. 굵고 둥근 소프트 라인, 스틱맨은 실제 골프 어드레스 자세 | 탄도각·수치 어시스트 비노출 — 골프의 매력 보존. **배경(데스크탑)은 사용자 영역 — 게임 디자인 적용 금지** (2026-08-13 교정) |
| 라이 | 페어웨이/러프 랜덤 밴드 + 에이프런·그린 — 굴림 마찰·반발·타격 페널티 차등 | 실제 골프의 지면 전략 재현 |
| 화면 경계 | 좌우 가장자리 벽 반사 | 캐주얼 대상에 관대한 규칙. 대안: OB 페널티 |
| 다중 모니터 | ~~MVP는 주 모니터만~~ → 메뉴 선택 지원 (2026-08-20) | ⛳️ 메뉴에서 선택·기억, 분리 시 주 모니터 폴백. 홀 진행 유지(월드가 미터 단위) |

## 재미 확장 4종 (2026-08-21 사용자 선택 — 전부 구현 완료)

1. **창 = 장애물(범퍼)**: 열린 앱 창에 비행 중인 공이 튕김 — 샷 순간 창 스냅샷(CGWindowList),
   GolfCore Bumper AABB 반사(240Hz 스텝이라 터널링 없음), 굴림 중엔 무시. 메뉴 토글(기본 ON)
2. **기록·도전과제·해금**: 누적 통계 + 배지 + 스틱맨 모자 해금 (UserDefaults)
3. **서프라이즈 이벤트**: 낮은 확률 랜덤 사건 — 새가 공 물어가 랜덤 드롭, 두더지 밀기
4. **바람**: 홀마다 풍향·풍속 → 항력·마그누스가 상대속도 기준으로, 깃발 펄럭임·HUD 표시

## 게임 설계

- **시그니처 홀** (2026-08-20 사용자 요청 → 실플레이 판정 2차 "모든 홀 전부 다이나믹"):
  **9홀 전부** 화면 세로 전체를 쓰는 다이나믹 지형, 아키타입은 덱 방식으로 라운드 내 4종
  골고루. 표준 평지 지형 경로(baseElevation)는 향후 클래식 모드용으로 코드 보존 —
  스카이 티(절벽 티샷)·산정 그린(벤치 등정)·대협곡(바닥 워터 60%)·계단 대지 4종.
  설계 원칙: 공이 설 수 있는 평탄 트레드(페어웨이) + 급경사 라이저(러프, 최대 경사 ≈0.95)의
  계단 조합 — 지속 급경사면은 굴림 물리상 공이 정지 불가라 계단이 정답. 표고 예산 0.34×worldW
  (16:9~21:9 화면 안, 32:9는 알려진 한계). 벙커·나무는 트레드에만.
- **코스**: 9홀, 파 합계 36 (파3×2·파4×5·파5×2 셔플). 홀 거리는 화이트 티 실거리, 화면 폭 = 홀 전장(홀마다 스케일 가변). 티~그린 사이 페어웨이/러프 랜덤 밴드, 그린 앞 에이프런. 라이별 물리: 굴림 마찰(그린 1.1 ~ 러프 4.5 m/s²)·바운스 반발·러프 타격 페널티(파워 ×0.75, 스핀 ×0.5).
- **클럽**: Driver, 3W, 5W, 3I–9I, PW, SW, Putter — 클럽별 로프트(발사각)·백스핀량·파워 스케일 테이블.
- **샷 입력**: `↑`/`↓`로 **스틱맨의 백스윙 높이** 조절(들어 올린 클럽이 곧 파워 게이지) → `←`/`→` 클럽 선택 → `Space` 다운스윙·타격 → `Esc` 일시정지/메뉴. 스틱맨 스윙 연출은 MVP 핵심 요소 (2026-08-13 사용자 교정 — 레퍼런스의 핵심 감성).
- **물리**: 중력 + 공기저항(속도 2차) + 마그누스 양력(백스핀) + 비행 중 스핀 감쇠 → 바운스(반발 계수 + 스핀 상호작용: 백스핀이면 체크/역주행) → 지면 롤(마찰). 퍼터는 순수 지면 롤. 상수는 실제 골프 데이터를 화면 스케일로 정규화하되 튜닝 가능하게 분리.
- **점수**: 홀별 타수, 거리 기반 파 산정 공식, 스코어카드, 라운드 합계(±파).
- **앱 형태**: 메뉴바 앱(`LSUIElement`). 라운드 중에만 오버레이 창 표시 + 키보드 캡처. 마우스는 항상 아래 앱으로 통과. 포커스 잃으면 자동 일시정지.

## 아키텍처

```
GolfCore  (순수 Swift, UI 의존 없음 — XCTest 대상)
 ├─ ClubTable        클럽 데이터 (로프트·스핀·파워)
 ├─ BallisticsEngine 탄도 시뮬레이션 (스텝 함수, 결정론적)
 ├─ HoleGenerator    시드 랜덤 홀 생성 + 파 산정
 └─ Scorecard        타수·점수 집계

App  (AppKit)
 ├─ OverlayWindow    투명 NSPanel: 항상 위, 클릭 통과, 비활성 키 캡처
 └─ MenuBarController 메뉴바 아이콘·라운드 시작/종료

Render  (SpriteKit)
 └─ GameScene        GolfCore 상태를 렌더만 (물리 미사용)
```

## 마일스톤

### 사전 단계 · HTML 게임필 프로토타입 ✅ (2026-08-13 사용자 요청으로 추가)
- [x] `prototype/mini-golf-prototype.html` — 탄도 물리·클럽 13종 밸런스·9홀 스코어링·스틱맨 스윙 연출을 브라우저에서 선검증
- [x] 랜덤 코스 시스템 (2026-08-13): 지형 고저(1m 샘플 하이트필드, 경사 법선 바운스·경사 굴림), 그린 브레이크(2~6%, 퍼팅 옵션 B), 벙커(플러그·탈출샷 페널티), 워터 해저드(1벌타+드롭)
- 확정된 튜닝: 재생 배속 `timeScale 2.5`, 클럽 비거리 DR 269m(캐리) ~ SW 69m, SW 롤아웃 ~1m(백스핀 체크 동작)
- 물리 상수·클럽 테이블은 GolfCore로 그대로 이식

### M0 · 리스크 스파이크 ✅ 통과 (2026-08-13, macOS 26 Tahoe 실기 검증)
- [x] 투명 NSPanel(.nonactivatingPanel, .statusBar 레벨) + SpriteKit `.clear` 씬 렌더 — 창 캡처·전체 화면 합성 모두 확인. 단 첫 합성까지 몇 초 지연 관찰(초기 렌더 타이밍, M1에서 재확인)
- [x] 키보드 캡처 — accessory 앱 + canBecomeKey 패널 조합으로 합성 키 이벤트(→×3)가 씬에 도달, 공 90pt 이동 실측
- [x] 포커스 상실 — 타 앱 활성화 후 키 이벤트가 오버레이에 도달하지 않음을 실측 (didResignKeyNotification 훅 연결됨). 클릭 통과는 ignoresMouseEvents API 보장(수동 확인 권장)
- 스파이크 코드: `Sources/MiniGolfSpike/main.swift` — 실행: `swift build && .build/debug/MiniGolfSpike` (Esc 종료)

### M1 · 개발 환경 + 코어 루프 ✅ 완료 (2026-08-13)
- [x] SwiftFormat 설정·통과, XCTest 14개 테스트 통과 (검증 루프 먼저)
- [x] GolfCore 이식: SeededRandom(mulberry32)·ClubTable·Surface·Course(지형·벙커·워터·브레이크)·Ballistics(경사 물리·립아웃) — 프로토타입과 동일 로직, 순수 Swift
- [x] 1홀 플레이 오버레이(`Sources/MiniGolf`): 실기에서 코스 렌더 + Space 스윙 → 공 비행 확인. 9홀 순회·점수 로직 포함
- 남은 것(M2로): 스틱맨·걷기·스윙 애니메이션 이식, HUD 칩 배경(밝은 배경 위 가독성), 스코어카드, 메뉴바 앱, 포커스 상실 일시정지 연결

### M2 · MVP — 9홀 + 점수 ✅ 완료 (2026-08-13)
- [x] 홀 생성 (시드 랜덤 + 파 산정) — `GolfCore/Course.swift`로 구현(별도 HoleGenerator 대신), 단위 테스트 포함
- [x] 9홀 순회, 스코어카드 HUD(`ScorecardNode`), 라운드 종료 화면
- [x] 메뉴바 앱 흐름: 시작/새 라운드/종료, 포커스 상실 자동 일시정지
- 추가(2026-08-14 머지): '조용한 계기판' HUD 재설계(상자 제거·타이포 중심·헤어라인 지형), 전역 단축키 ⌃⇧G(Carbon 핫키, 권한 불필요), 퍼터 거리 프리셋·홀인 창 확대

### M3 · 폴리시 — 게이트 통과 (2026-08-14)
- [x] 비주얼 방향 정의 후 적용 — '조용한 계기판' 방향으로 HUD 재설계 완료 (feature/quiet-hud)
- [x] 사운드(타격·홀인·휘시·착지·립아웃·워터·차임 — 합성음 SoundKit)·궤적 이펙트·미세 파티클 FX (feature/m3-sound-fx). 사용자 청음 튜닝 피드백은 열려 있음
- [x] **/design-review 게이트 통과** — 발견 4건(critical 1: 밝은 배경 시인성) 전부 수정, Design Score A- · AI Slop A. 보고서: `~/.gstack/projects/mini-golf/designs/design-audit-20260814/`. 밝은 배경 대응은 **'고대비 모드' 토글(기본 꺼짐)** 로 확정 — 상시 듀얼톤은 이질감(사용자 판정)이라 기본값은 원래 디자인 유지. Palette 상수 도입. M4 README에 고대비 모드 안내 필요

### M4 · 오픈소스 공개 — [게이트: 공개 전 사용자 최종 확인]
- [ ] README (빌드·실행 방법, 미서명 실행 `xattr` 안내), 데모 GIF, MIT LICENSE, CHANGELOG
- [ ] 저장소 public 전환 + GitHub Release

## 검증 계획

각 마일스톤: `swift build` → `swiftformat --lint .` → `swift test` → 수동 실행 확인. 완료 보고에 실행 결과 포함.
탄도 엔진은 결정론적 스텝 함수로 설계해 물리 회귀를 단위 테스트로 방지.

## 리스크

| 리스크 | 대응 |
|---|---|
| SpriteKit 투명 배경 함정 (`skView.backgroundColor` 설정 시 투명 깨짐) | M0에서 검증, 코드 주석으로 명시 |
| macOS 최신 버전 오버레이 회귀 (Sonoma `setNeedsDisplay` 클릭 통과 이슈 등) | M0에서 실기 검증 |
| 키보드 캡처 UX — 라운드 중 다른 앱에 키 유출, 포커스 뺏김/잃음 | NSPanel nonactivating 방식, M0 검증 + 자동 일시정지 |
| 물리 튜닝이 재미를 좌우 | 상수를 GolfCore 테이블로 분리, 테스트로 회귀 방지, 튜닝 반복 |
| 미서명 배포 마찰 (macOS 15.1+) | README `xattr` 안내, 추후 Homebrew cask |
